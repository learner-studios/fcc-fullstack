var a = '3';
var b = '8';

// so convert change the variables so that way the strings are changed:

var c = a;
a = b;
b = c;

// outputting the results
console.log(`a is ${a}`);
console.log(`b is ${b}`);